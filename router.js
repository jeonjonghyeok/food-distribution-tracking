// global

const HOME = "/";
const SIGNIN = "/signin";
const LOGIN = "/login";
const LOGOUT = "/logout";
const BBQ = "/bbq";
const HTRUCK = "/htruck";

// users

const USERS = "/users";

const routes = {
    home: HOME,
    signin: SIGNIN,
    login: LOGIN,
    logout: LOGOUT,
    users : USERS,
    bbq : BBQ,
    htruck : HTRUCK
};


export default routes;